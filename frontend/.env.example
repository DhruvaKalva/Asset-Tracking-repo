# Leave empty to use the Vite dev proxy (recommended in dev).
# Set to an absolute origin when the frontend is served separately from the API.
VITE_API_BASE=

# Where the dev proxy forwards /api. Defaults to http://127.0.0.1:8000
VITE_PROXY_TARGET=http://127.0.0.1:8000
